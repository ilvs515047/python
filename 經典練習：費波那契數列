# 經典練習：費波那契數列
# 費波那契數列是一個數學序列，其中每個數字是前兩個數字的和，從 0 和 1 開始。
# 例如：0, 1, 1, 2, 3, 5, 8, 13, 21, ...

# 請在這裡完成您的練習：
# 1. 定義一個變數 `n`，代表您想生成費波那契數列的長度。
# 2. 使用迴圈生成並列印出前 `n` 個費波那契數字。
n = 10
a = []
for lo in range(n):
    
    a.append(lo)  # 將結果加入串列
for i in range(n):
  if i == 0:
    i=2
  if i == 1:
    i=2
  a[i]=a[i-2]+a[i-1]
print(a)

